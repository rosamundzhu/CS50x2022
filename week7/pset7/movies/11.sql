SELECT title FROM movies, ratings WHERE movies.id IN (SELECT movie_id FROM stars, people WHERE people.id = stars.person_id AND name = "Chadwick Boseman") AND ratings.movie_id = movies.id ORDER BY rating DESC LIMIT 5;